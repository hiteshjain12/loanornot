# robots.txt for loanornot
# Allow all search engines to crawl everything except the .git directory
User-agent: *
Disallow: /.git/

# Allow everything else
Allow: /

# Sitemap
Sitemap: https://fin-calculators.vercel.app/sitemap.xml 